<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- White application theme. -->
    <style name="Theme.CashCard" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>

        <!-- Button colors -->
        <item name="colorButtonNormal">@color/purple_200</item>
        <item name="colorControlHighlight">@color/purple_700</item>
        <item name="colorAccent">@color/purple_700</item>

        <!-- Customize your theme here. -->
    </style>
</resources>

